
namespace EnumSpace
{
    public enum PlayerState { IDLE, RUN, ATTACK, DIE };
    public enum PlayerType { MELEE, RANGE };
    public enum EnemyState { IDLE, TRACE, ATTACK, DIE, GAUGING, };
    public enum EnemyType { MONSTER, GHOST_RED, GHOST_BLUE };
}
